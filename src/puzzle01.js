const s1 = 'SEND';
const s2 = 'MORE';
const s3 = 'MONEY';

const letters = 'SENDMORY';

const numbers = [0,2,3,4,5,6,7,8,9]

for (let l in letters) {
  console.log(letters[l])
}

for (let n in numbers) {
  console.log(numbers[n])
}
